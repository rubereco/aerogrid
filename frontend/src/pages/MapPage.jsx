/**
 * Main map component page.
 * Acts as the primary secured interface after successful authentication.
 *
 * @returns {JSX.Element}
 */
export default function MapPage() {
    return (
        <div className="p-10">
            <h1 className="text-2xl font-bold">El Mapa Principal 🌍</h1>
            <p>Aquí carregarem les estacions i Leaflet.</p>
        </div>
    );
}